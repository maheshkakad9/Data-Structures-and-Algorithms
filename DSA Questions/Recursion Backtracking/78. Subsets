class Solution {
    public List<List<Integer>> subsets(int[] nums) {
        List<List<Integer>> res = new ArrayList<>();
        List<Integer> subset = new ArrayList<>();

        createSubset(nums,0,res,subset);
        return res;
    }
    private void createSubset(int[] nums, int index, List<List<Integer>> res,List<Integer> subset) {
        if (index == nums.length) {
            res.add(new ArrayList<>(subset));
            return;
        }

        subset.add(nums[index]);  //Make choice include 
        createSubset(nums,index+1,res,subset);   //Recursion with choice made

        subset.remove(subset.size() - 1);  //Undo choice(Backtrack)
        createSubset(nums, index + 1, res, subset);  //Recurse with different choice 
    }
}
